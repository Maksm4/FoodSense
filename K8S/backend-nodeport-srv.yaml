apiVersion: v1
kind: Service
metadata:
  name: node-port-srv
spec:
  type: NodePort
  selector:
    app: inventory-api
  ports:
    - name: inventory-api
      port: 80
      protocol: TCP
      targetPort: 80