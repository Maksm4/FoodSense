apiVersion: v1
kind: Service
metadata:
  name: node-port-srv
spec:
  selector:
    app: inventory-api
  ports:
    - name: inventory-api
      port: 80
      protocol: TCP
      targetPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: auth-srv
spec:
  selector:
    app: auth-api
  ports:
    - name: auth-port
      port: 80
      targetPort: 8080